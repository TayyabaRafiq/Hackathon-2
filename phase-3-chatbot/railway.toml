[build]
builder = "nixpacks"
buildCommand = "cd phase-3-chatbot/backend && npm ci && npm run build"

[deploy]
startCommand = "cd phase-3-chatbot/backend && npm run db:migrate && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
